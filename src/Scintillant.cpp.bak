#include "Scintillant.h"
#include <iostream>
using namespace std;

const bool HitMap::getX(uint32_t id) const {
    uint8_t segment  = id/32;
    uint8_t position = id%32;

    cout << "segment: " << segment << " position: " << position << endl;
    return (m_x[id/32]>>(id%32-1))&0x1;
}
